generator client {
  provider   = "prisma-client-js"
  engineType = "library"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://web_calender_database_user:RWado4VY7azN5sIx5gf8XCyoqhvy90aU@dpg-cq26fijv2p9s73enhjeg-a.frankfurt-postgres.render.com/web_calender_database"
}

model gone {
  idGone        Int      @default(autoincrement())
  StartDate     DateTime @db.Date
  EndDate       DateTime @db.Date
  Reason        String?  @db.Text
  Colour        String?  @db.VarChar(9)

  Name          String  @db.VarChar(45)
  users         users    @relation(fields: [Name], references: [Name], onDelete: NoAction, onUpdate: NoAction, map: "fk_Gone_Users")

  @@id([idGone, Name])
  @@index([Name], map: "fk_Gone_Users_idx")
}

model users {
  Name     String @id @db.VarChar(45)
  Password String? @db.VarChar(45)
  gone     gone[]
}
